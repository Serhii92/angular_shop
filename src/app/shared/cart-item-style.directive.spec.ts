import { CartItemStyleDirective } from './cart-item-style.directive';

describe('CartItemStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CartItemStyleDirective();
    expect(directive).toBeTruthy();
  });
});
